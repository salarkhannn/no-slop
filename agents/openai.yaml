interface:
  display_name: "No Slop"
  short_description: "Build distinctive, intentional web interfaces"
  default_prompt: "Use $no-slop to establish a product-specific governing idea, choose components and visual media intentionally, then implement and verify the interface in context."
