interface:
  display_name: "No Slop"
  short_description: "Build distinctive, high-craft web interfaces"
  default_prompt: "Use $no-slop to challenge this codebase's visual defaults—including its typeface, weight hierarchy, and unnecessary containment—then compose and verify a product-specific interface."
